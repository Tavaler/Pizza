@model IEnumerable<Pizza.Models.User>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout01.cshtml";
}

<h2 style="margin-top:100px">Index</h2>

<p>
    <button type="button" class="btn btn-outline-success">@Html.ActionLink("Create New", "Create")</button>
</p>
<br>
    <table class="table table-bordered table-hover">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User_Password)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User_Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User_Phone)
            </th>
            <th></th>
            <th></th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.User_ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Title.Title_Name)@Html.DisplayFor(modelItem => item.User_Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User_Password)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User_Address)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User_Phone)
                </td>
                <td>
                </td>
                <td>
                <td>
                    <button type="button" class="btn btn-outline-info">@Html.ActionLink("Edit", "Edit", new { id = item.User_ID })</button>
                    <button type="button" class="btn btn-outline-danger">@Html.ActionLink("Delete", "Delete", new { id = item.User_ID })</button>
                </td>
            </tr>
        }

    </table>



    @*@model IEnumerable<Pizza.Models.User>

        @{
            ViewBag.Title = "Index";
            Layout = "~/Views/Shared/_Layout01.cshtml";
        }

        <h2>Index</h2>

        <p>
            @Html.ActionLink("Create New", "Create")
        </p>
        <table class="table">
            <tr style="background-color:cornflowerblue">
                <td>
                    ไอดีผู้ใช้
                </td>
                <th>
                    ชื่อผู้ใช้
                </th>
                <th>
                    รหัส
                </th>
                <th>
                    ที่อยู่
                </th>
                <th>
                    เบอร์มือถือ
                </th>
                <th></th>
                <th></th>

            </tr>

            @foreach (var item in Model)
            {
                <tr style="background-color:aquamarine">
                    <td>
                        @Html.DisplayFor(modelItem => item.User_Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Title.Title_Name)@Html.DisplayFor(modelItem => item.User_Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.User_Password)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.User_Address)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.User_Phone)
                    </td>
                    <td>
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.User_Id }) |
                        @Html.ActionLink("Details", "Details", new { id = item.User_Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.User_Id })
                    </td>
                </tr>
            }

        </table>*@
